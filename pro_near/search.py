# load in progs
# run near with randomly picked hole (last node??)
#and time constraint
# and then.... 
from random_search import Subtree_search
import glob
for prog in glob glob programs:
    search_instance = Subtree_search(**vars(args))
    # run with it as base_program, 